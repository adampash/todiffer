<header>
<h2><%= header_link %></h2>
<div class="actions">
  <% if user_signed_in? %>
    <%= current_user.name %>
    <%= link_to "logout", logout_path %>
  <% else %>
    <%= link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2) %>
  <% end %>
</div>
</header>
