<h1><%= @site.name %></h1>
<h2><%= @site.twitter %></h2>
<ul>
  <% @texts.each do |text| %>
    <li>
      <%= link_to text.title, text_path(text) %>
      Versions: <%= text.version_count %>
    </li>
  <% end %>
</ul>
